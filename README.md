# dribbble

Para utilizar a aplicação:
 > no console execute:
 > npm install (para instalar os pacotes e as dependências da app).
 > npm start (servidor webpack vai ser executado).

 > localhost:9999
 
Existem algumas partes que eu não consegui terminar. 

;(..